import { __name } from '../chunk-AZHX2MBF.mjs';
import { isObject } from './isObject.mjs';

function objectToTuples(obj, prefix = "") {
  const entries = [];
  for (const [key, value] of Object.entries(obj)) {
    if (isObject(value)) {
      entries.push(...objectToTuples(value, `${prefix}${key}.`));
    } else {
      entries.push([`${prefix}${key}`, value]);
    }
  }
  return entries;
}
__name(objectToTuples, "objectToTuples");

export { objectToTuples };
//# sourceMappingURL=objectToTuples.mjs.map