'use strict';

var dmChannel_js = require('../lib/resolvers/dmChannel.js');
var Argument_js = require('../lib/structures/Argument.js');

var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
var CoreArgument = class extends Argument_js.Argument {
  constructor(context) {
    super(context, { name: "dmChannel" });
  }
  run(parameter, context) {
    const resolved = dmChannel_js.resolveDMChannel(parameter, context.message);
    return resolved.mapErrInto(
      (identifier) => this.error({
        parameter,
        identifier,
        message: "The argument did not resolve to a DM channel.",
        context
      })
    );
  }
};
__name(CoreArgument, "CoreArgument");

exports.CoreArgument = CoreArgument;
//# sourceMappingURL=out.js.map
//# sourceMappingURL=CoreDMChannel.js.map