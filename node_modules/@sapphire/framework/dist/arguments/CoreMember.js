'use strict';

var Identifiers_js = require('../lib/errors/Identifiers.js');
var member_js = require('../lib/resolvers/member.js');
var Argument_js = require('../lib/structures/Argument.js');

var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
var CoreArgument = class extends Argument_js.Argument {
  constructor(context) {
    super(context, { name: "member" });
  }
  async run(parameter, context) {
    const { guild } = context.message;
    if (!guild) {
      return this.error({
        parameter,
        identifier: Identifiers_js.Identifiers.ArgumentMemberMissingGuild,
        message: "This command can only be used in a server.",
        context
      });
    }
    const resolved = await member_js.resolveMember(parameter, guild);
    return resolved.mapErrInto(
      (identifier) => this.error({
        parameter,
        identifier,
        message: "The given argument did not resolve to a server member.",
        context: { ...context, guild }
      })
    );
  }
};
__name(CoreArgument, "CoreArgument");

exports.CoreArgument = CoreArgument;
//# sourceMappingURL=out.js.map
//# sourceMappingURL=CoreMember.js.map